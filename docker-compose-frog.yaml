services:
  backend:
    build: .
    container_name: studify-backend
    restart: always
    env_file:
      - ../environment/prod/backend/backend.env
    ports:
      - "80:8080"
    networks:
      - frog-net

networks:
  frog-net:
    external: true